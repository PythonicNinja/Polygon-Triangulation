============
Installation
============

At the command line::

    $ easy_install PolygonTriangulation

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv PolygonTriangulation
    $ pip install PolygonTriangulation
